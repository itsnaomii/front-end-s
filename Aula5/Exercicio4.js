//4- crie duas funções assincronas que retornem Promises simulando a obtenção de
//dados diferentes. Use Promise.All para obter ambos os resultados simultaneamente//
function dados() {
  return new Promise((resolve) => {
    setTimeout(() => resolve(), 1000)
  })
}

 function dados() {
  return new Promise((resolve) => {
    setTimeout(() => resolve(), 1000)
  })
 }
 
 async function myAsyncFunction() {
  let  
  return await Promise.all() =>
 }