/* Crie uma classe chamada "Retangulo" que possua atributos para armazenar 
a largura e altura. implemente metodos para calcular a area e o perimetro 
do retangulo*/

class Retangulo {
  constructor(largura, altura) {
    this.largura = largura
    this.altura = altura
    this.retangulo = 0
  }
  metros(largura) {
    if (largura > 0) {
      this.largura = retangulo

      console.log(`O retangulo tem ${retangulo.toFixed(2)} de largura`)
    }
  }

  comprimento(altura) {
    if (altura > 0) {
      this.altura = retangulo

      console.log(`O retangulo tem ${altura.toFixed(2)} de altura`)
    }
  }
}
 calculoDaAreaPerimetro() {
    console.log(
        `
    )
 }